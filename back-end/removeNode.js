// Remove a Node

function removeNode(network_name, node_number) {
    
    //run docker command to stop and remove container
    
    const node = (network_name + node_number)
    
    // DOCKER -> docker stop node
    // DOCKER -> docker rm node

    return 
}