// Create Genesis File

function createGenesis(network_id, faucet_address) {

    // Read from template ./genesis_template.json

    // Replace chain_id with network_id

    // Create alloc element with faucet_address

    // Store new genesis.json file and return ok

    return genesis_name

}